class Solution:
    def isPalindrome(self, s: str) -> bool:
        clean = ''.join(c.lower()for c in s if c.isalnum())
        if clean == clean[::-1]:
            return True
        else:
            return False